커밋(Commit) : 로컬 저장소에 저장
푸시(Push) : 원격 저장소에 저장
패치(fetch) : 원격 저장소의 기록을 로컬 저장소에 받기
풀(Pull) : 원격 저장소의 파일 받기

브랜치(Branch) : 분기(main을 복사한)를 만드는 것
병합(Merge) : 브랜치와 브랜치를 합치는 것
충돌(Conflict) : 병합을 실행할 시 같은 파일을 수정한 경우 에러가 나지 않도록 해결하는 것

<main에 다른 브랜치 병합시의 순서>

기능브랜치에 main을 병합 -> 충돌 발생시 해결 -> main에 기능브랜치 병합
